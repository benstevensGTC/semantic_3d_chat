_base_: embodied_live.yaml

# Fresh namespace for the collision-limited safe-approach successor. Historical
# V2 artifacts remain immutable evidence of the scene-000031 collision failure.
paths:
  data_root: data_gemma4/runtime/embodied_hybrid_approach_chair_v3_validation
