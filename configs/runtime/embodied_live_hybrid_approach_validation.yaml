_base_: embodied_live.yaml

# Isolate finite current-runtime approach validation from the face-alignment
# evidence and from the persistent map used by the interactive demo.
paths:
  data_root: data_gemma4/runtime/embodied_hybrid_approach_validation
