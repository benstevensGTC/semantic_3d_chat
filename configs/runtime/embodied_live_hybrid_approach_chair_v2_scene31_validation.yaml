_base_: embodied_live.yaml

# Isolated persistent-map namespace for the second-scene 0.5 m safe-standoff
# approach validation. This preserves the first-scene evidence unchanged.
paths:
  data_root: data_gemma4/runtime/embodied_hybrid_approach_chair_v2_scene31_validation
