_base_: embodied_live.yaml

# Independent second-scene namespace for the collision-limited safe-approach
# successor; no persistent observation from another run can be reused.
paths:
  data_root: data_gemma4/runtime/embodied_hybrid_approach_chair_v3_scene31_validation
